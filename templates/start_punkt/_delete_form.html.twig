<form method="post" action="{{ path('start_punkt_delete', {'id': start_punkt.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ start_punkt.id) }}">
    <button class="btn">Delete</button>
</form>
